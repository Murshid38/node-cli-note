export {other} from './other.js'
export {count, somethingElse} from './utils.js'
// above two lines are equivalent to importing them from the locations given in the path and immediately exporting them
// now when we use 'import * as utils(the name we are giving) from './utils'
